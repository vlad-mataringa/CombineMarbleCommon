import XCTest
@testable import CombineMarbleCommon

final class CombineMarbleCommonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CombineMarbleCommon().text, "Hello, World!")
    }
}
